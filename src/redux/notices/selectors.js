export const selectIsLoading = state => state.notices.isLoading;
export const selectNotice = state => state.notices.items;
export const selectError = state => state.notices.error;
export const selectNoticeById = state => state.notices.selectedNotice;
